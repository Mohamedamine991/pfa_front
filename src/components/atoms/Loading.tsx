const Loading = () => {
  return (
    <div className="w-full h-screen bg-white flex items-center justify-center fixed top-0 z-50">
      <span className="loader"></span>
    </div>
  )
}

export default Loading
